{
  "name": "farmers-market-mvp",
  "version": "1.0.0",
  "description": "Farmers' Direct Market Access MVP - Connecting farmers directly with buyers",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["farmers", "marketplace", "agriculture", "mvp"],
  "author": "SIH Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}